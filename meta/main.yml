galaxy_info:
  author: Joe Nyland
  role_name: dropbear_initramfs
  namespace: joenyland
  description: Installs dropbear-initramfs and configures it
  license: MIT
  min_ansible_version: '2.1'
  platforms:
  - name: Debian
    versions:
      - bullseye # 11 (stable)
      - buster # 10 (oldstable)
      - stretch # 9 LTS (oldoldstable)
  galaxy_tags:
    - debian
    - dropbear
    - initramfs
dependencies: []
